# ============================================================================
# Regulatory Compliance RAG Assistant - Package Dependencies
# ============================================================================
# Python Version: 3.8, 3.9, 3.10, or 3.11 (3.11 recommended)
# Project: Regulatory Compliance RAG Assistant
# Description: Advanced RAG system for financial compliance teams
#              - Hybrid retrieval (ChromaDB + BM25)
#              - Cross-encoder reranking
#              - Multi-jurisdictional support (RBI, Basel, SEBI)
#              - Streamlit web interface
#              - Automated evaluation pipeline
# Installation:
#   pip install -r requirements.txt
# Alternative (for faster CPU-only installation):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# ============================================================================

# ============================================================================
# CATEGORY 1: CORE DEPENDENCIES
# Essential packages for Python development and file operations
# ============================================================================

# Numpy: Numerical computing foundation for all ML libraries
# Purpose: Array operations, matrix math, embedding calculations
# Why: Standard requirement for handling underlying tensor data arrays efficiently
# Features: Multi-dimensional array objects, linear algebra routines
# Installation: pip install numpy
numpy==1.24.3

# Pandas: Data manipulation and analysis for evaluation metrics
# Purpose: DataFrame operations for metrics CSV and evaluation results
# Why: Version 2.0+ delivers massive performance improvements and native Apache Arrow backend options
# Features: Tabular data alignment, fast I/O operations, seamless integration with Plotly
# Installation: pip install pandas
pandas==2.2.1

# ============================================================================
# CATEGORY 2: PDF PROCESSING & TEXT EXTRACTION
# Packages for parsing regulatory PDF documents
# ============================================================================

# pdfplumber: PDF text extraction with better handling than PyPDF2
# Purpose: Parse regulatory PDFs, extract text per page, handle complex layouts
# Why: Drastically outperforms PyPDF2 for multi-column structures and tables embedded in complex regulatory papers
# Features: Visual debugging tracking, detailed metadata tracking per character, precise positioning
# Installation: pip install pdfplumber
pdfplumber==0.7.1

# PyPDF2: Alternative PDF parser (fallback if pdfplumber fails)
# Purpose: Backup PDF parsing, structural verification, and metadata extraction
# Why: Used strictly as a resilient fallback layer in ingest.py if structural font issues stall pdfplumber
# Features: Lightweight document stream splitting, file decryption utilities
# Installation: pip install pypdf2
pypdf2==3.0.1

# ============================================================================
# CATEGORY 3: EMBEDDINGS & VECTOR DATABASE
# Dense vector embeddings and ChromaDB persistent vector store
# ============================================================================

# ChromaDB: Lightweight, persistent vector database for embeddings
# Purpose: Store and query dense embeddings for semantic chunk searches
# Why: Python-native, zero external server management required, perfect for fast compliance auditing prototyping
# Features: Built-in SQLite persistence (chroma.sqlite3), rich metadata filtering, automatic indexing
# Installation: pip install chromadb
chromadb==0.4.22

# sentence-transformers: Sentence embedding model library
# Purpose: Generate high-quality dense vector representations using all-MiniLM-L6-v2
# Why: Industry gold-standard for semantic retrieval, mapping text to a continuous vector space
# Features: Out-of-the-box support for Siamese networks, automatic token length truncation management
# Installation: pip install sentence-transformers
sentence-transformers==2.3.1

# ============================================================================
# CATEGORY 4: SPARSE KEYWORD SEARCH (BM25)
# BM25 algorithm for exact term matching
# ============================================================================

# rank_bm25: Python implementation of BM25 sparse keyword algorithm
# Purpose: Token matching for strict elements (dates, rule codes, numeric financial thresholds)
# Why: Complements dense semantic vectors by ensuring exact matches on technical strings like "Section 138"
# Features: Tokenized document index score calculation using frequency-inverse document weights
# Installation: pip install rank-bm25
rank-bm25==0.2.2

# ============================================================================
# CATEGORY 5: DEEP LEARNING & TRANSFORMERS
# Core ML libraries for embeddings and reranking
# ============================================================================

# Transformers: Hugging Face Transformers library
# Purpose: Low-level access configuration for pre-trained language and tokenization layers
# Why: Backing foundation powering downstream cross-encoders and customized pipelines
# Features: Centralized caching structure, uniform execution layer for tokenizers and configuration objects
# Installation: pip install transformers
transformers==4.38.0

# Torch: PyTorch deep learning framework
# Purpose: Core mathematical computation engine backing sentence embeddings and model inference
# Why: Essential matrix-processing framework required across both local CPU processing and CUDA GPU servers
# Features: Dynamic graph definition, hardware acceleration abstraction layer
# Installation: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.2.0

# ============================================================================
# CATEGORY 6: CROSS-ENCODER RERANKING
# Cross-encoder models for precision ranking
# ============================================================================

# cross-encoder: Cross-encoder reranking library
# Purpose: Jointly score query-document pairs to maximize retrieval hit metrics
# Why: Boosts Hit Rate@5 by up to 20% compared to pure cosine-similarity metrics
# Note: Packaged automatically within sentence-transformers, pinned explicitly below for visibility
# Installation: Included directly with sentence-transformers setup
# cross-encoder==0.6.1

# ============================================================================
# CATEGORY 7: LLM INTEGRATION (OPENAI & OLLAMA)
# Cloud and local LLM providers for answer generation
# ============================================================================

# OpenAI: OpenAI Python SDK for GPT models
# Purpose: Production-grade cloud inference interfacing for compliance validation
# Why: Industry-leading logical verification capacities needed to parsing conflicting compliance codes
# Features: Native completion streaming parameters, robust retry mechanisms
# Installation: pip install openai
openai==1.12.0

# Ollama: Local LLM inference client
# Purpose: On-premise air-gapped text generation pipeline avoiding sensitive telemetry export
# Why: Completely free, offline operation, respects rigorous organizational privacy profiles
# Features: Standardized REST endpoint wraps, seamless connection orchestration to local daemon
# Installation: pip install ollama
ollama==0.1.7

# ============================================================================
# CATEGORY 8: WEB FRAMEWORK (STREAMLIT)
# Streamlit for rapid web UI development
# ============================================================================

# Streamlit: Python web framework for data applications
# Purpose: Present interactive application UI directly to legal auditor stakeholders without JS engineering
# Why: Translates stateful python scripts instantly into intuitive graphical reactive user view layouts
# Features: Hot reloading runtime architecture, session state caching wrappers, streaming chunk renderers
# Installation: pip install streamlit
streamlit==1.32.0

# ============================================================================
# CATEGORY 9: EVALUATION & VISUALIZATION
# Plotly charts and Pandas for evaluation metrics
# ============================================================================

# Plotly: Interactive graphing library for evaluation charts
# Purpose: Generate interactive data reports covering Hit Rate and Mean Reciprocal Rank parameters
# Why: Delivers clean visual telemetry directly within web frameworks without bulky layout layers
# Features: Plotly Express rapid prototyping wrapper, direct engine exports to vector images
# Installation: pip install plotly
plotly==5.19.0

# ============================================================================
# CATEGORY 10: UTILITIES & CONFIGURATION
# Environment variables, logging, and helper utilities
# ============================================================================

# python-dotenv: Environment variable management
# Purpose: Load API credentials and engine configurations from hidden local file layers
# Why: Keeps production credential keys separated from base repositories to satisfy enterprise workflows
# Features: Transparent local parsing engine updating basic environment maps automatically
# Installation: pip install python-dotenv
python-dotenv==1.0.1

# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# CPU-only Installation (recommended for most environments):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# GPU Installation (for accelerated local embeddings/reranking pipelines):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# Optional: OpenAI Setup:
#   pip install openai
#   export OPENAI_API_KEY="your-api-key"
#
# Optional: Local Ollama Setup:
#   Download from: https://ollama.ai
#   Pull targeted local model: ollama pull llama3
#   Start background worker: ollama serve
#
# Troubleshooting Guidelines:
#   - If ChromaDB compilation fails: Ensure a C++ compiler build toolset is configured, or try: pip install chromadb --upgrade
#   - If sentence-transformers compilation hangs: Verify explicit CPU binaries match: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   - If Streamlit execution routes fail: Reset presentation structures: pip install streamlit --upgrade
#   - If pdfplumber layouts crash: Install required image renderers, or execute: pip install pdfplumber --upgrade
#
# Verification Script:
#   python -c "import chromadb; import streamlit; import sentence_transformers; import rank_bm25; import openai; import ollama; import plotly; import pandas; print('✅ All dependencies verified')"
# ============================================================================